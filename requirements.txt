# FastAPI
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.0
pydantic-settings==2.1.0
python-multipart==0.0.6
python-dotenv==1.0.0

# 문서 처리
pypdf==3.17.4
python-docx==1.1.0

# AWS SDK (Bedrock 임베딩)
boto3==1.35.0

# Vector DB
chromadb==0.5.3  # 레거시 (로컬 개발용)
pgvector==0.2.4  # PostgreSQL 벡터 확장 (AWS 프로덕션용)

# LangChain (텍스트 분할 전용)
langchain==0.1.0
langchain-community==0.0.13
tiktoken==0.5.2

# LLM 클라이언트
openai==1.54.0
anthropic==0.38.0  # httpx 0.27.x 호환 버전 (0.39.0+는 httpx 0.28+ 필요, chromadb는 httpx>=0.27.0 필요)
httpx==0.27.2
langchain-openai==0.0.5
google-generativeai==0.8.3

# 유틸리티
aiofiles==23.2.1
tenacity==8.2.3
itsdangerous==2.1.2  # SessionMiddleware 필수
slowapi==0.1.9  # Rate limiting

# Redis
redis==5.0.1

# Slack SDK
slack-sdk==3.38.0

# 암호화 (Slack OAuth token encryption)
cryptography==42.0.8

# 데이터베이스
sqlalchemy==2.0.25
alembic==1.13.1
asyncpg==0.29.0
psycopg2-binary==2.9.9  # Alembic migration용 (sync driver)

# 인증
authlib==1.3.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
email-validator==2.1.0
